/*
 * task_ble.c
 *
 *  Created on: Mar 29, 2021
 *      Author: Arkadiusz
 */

//#include "task_ble.h"
#include "bluetooth_le.h"
#include <stdio.h>
// working all the time, checking if something is received or sending data to android device
void task_ble(void *pvParameters) {

//	ble_init();
//	ble_test_command();
//	char *pData = (char*) malloc(sizeof(char) * 20 * 4);
//	char pData[20 * 4];
//	if (pData == NULL) {
//		while (1)
//			;
//	}
////	char *pString = (char*) malloc(sizeof(char) * 20 * 4);
//	char pString[20 * 4];
//	if (pString == NULL) {
//		while (1)
//			;
//	}
//	(void*) pvParameters;
////	ble_turn_off_transmittion;
//	ble_turn_on_transmittion;
//
//	int no = sprintf(pString, "AT+NAME=Wireless-tag05\r");
//	ble_send_string(pString, no);
//	memset(pString, 0, strlen(pString));
//	no = sprintf(pString, "AT+RX\r");
//	ble_receive_string(pString, pData);
//
//	ble_turn_off_transmittion;
//	ble_turn_on_transmittion;

//
//	no = sprintf(pString, "AT + NAME = Wireless-tag02\r");
//	ble_send_string(pString, no);
//	no = sprintf(pString, "AT + NAME = Wireless-tag03 \r");
//	ble_send_string(pString, no);
//	no = sprintf(pString, "AT + NAME = Wireless-tag04 \ r");
//	ble_send_string(pString, no);

//	int no = sprintf(pData, "AT+NAME=Wireless-tag04\r");
//	no = sprintf(pString, "AT+RX\r");
//	ble_receive_string(pString, pData);
//
//
//	ble_turn_off_transmittion;
//
//	no = sprintf(pString, "AT+NAME=Wireless-tag05\r");
//	ble_send_string(pString, no);
//	no = sprintf(pString, "AT + NAME = Wireless-tag06\r");
//	ble_send_string(pString, no);
//	no = sprintf(pString, "AT + NAME = Wireless-tag07 \r");
//	ble_send_string(pString, no);
//	no = sprintf(pString, "AT + NAME = Wireless-tag08 \ r");
//	ble_send_string(pString, no);

//	int no = sprintf(pData, "AT+NAME=Wireless-tag04\r");
//	no = sprintf(pString, "AT+RX\r");
//	ble_receive_string(pString, pData);
	//	char *strcpy (char* strTo, const char* strFrom);

//	strcpy(pString, pData);

//	ble_send_string("AT+NAME=Wireless-tag01");
//	ble_send_string("AT + NAME=Wireless-tag02");
//	ble_send_string("AT+NAME = Wireless-tag03");
//	ble_send_string("AT+NAME=Wireless-tag04\r");
//	ble_receive_string("AT + RX \ r", pData);
//	ble_receive_string("AT+RX\r", pData);
//	ble_turn_off_transmittion;
//	ble_send_string("AT+NAME=Wireless-tag05");
//	ble_send_string("AT+NAME=Wireless-tag06\r");
//	ble_receive_string("AT + RX \ r", pData);
//	ble_receive_string("AT+RX\r", pData);

//	free(pData);
//	AT + RX \ r
//	int i = 0;
	for (;;) {
//		int length = strlen()

	}

}
